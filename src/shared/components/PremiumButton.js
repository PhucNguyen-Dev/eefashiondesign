/**
 * Premium Button Component - Legacy wrapper
 * This component is now a wrapper around the Tamagui PremiumButton
 * For new code, import directly from '@/components/tamagui'
 */

import React from 'react';
import PremiumButtonTamagui from '../../../components/tamagui/PremiumButton';

  return <PremiumButtonTamagui />;

export default PremiumButton;

