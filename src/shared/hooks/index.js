/**
 * Shared Hooks Index
 * Export all shared/reusable hooks
 * 
 * Note: useAnimation.ts is now in src/hooks/useAnimation.ts
 * Import from there for the TypeScript version
 */

// Re-export from main hooks directory (TypeScript version)
export * from '../../hooks/useAnimation';

